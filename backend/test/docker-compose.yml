version: '3'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example
    ports:
      - 5432:5432


  api:
    build: images/api
    restart: always
    volumes:
      - "../volumes/api/flask:/app"
    ports:
      - 5000:5000